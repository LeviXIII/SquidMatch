import React, { Component } from 'react';
import SiteHeader from './SiteHeader';

class Home extends Component {
    render() {
        return(
            <SiteHeader />
        );
    }
}

export default Home;